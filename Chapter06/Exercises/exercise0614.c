/*
    if(n % 2 == 0);
        printf("n is even.\n");

    The error is the semicolon after the if statement. The printf statement will always be executed even if n is not even number.
    To correct the program just remove the semicolon:
    if(n % 2 == 0)
        printf("n is even.\n");
*/